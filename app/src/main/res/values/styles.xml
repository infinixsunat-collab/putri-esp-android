<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@color/green</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
     <style name="myBtn">
		<item name="android:colorControlNormal">#40000000</item>
        <item name="android:colorControlActivated">#FF000000</item>
    </style>
	<style name="custom_seekbar" parent="@android:style/Widget.SeekBar">
        <item name="android:maxHeight">8dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:minHeight">8dp</item>
        <item name="android:thumb">@android:color/transparent</item>
        <item name="android:thumbOffset">10dp</item>
    </style>
    
        <style name="SCBSwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">#FF2196F3</item>
        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">#FF424242
        </item>
        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">#FF424242
        </item>
        
    </style>

</resources>
